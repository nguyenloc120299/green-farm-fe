import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  "appId": "com.greenfarm.app",
  "appName": "Green Farm",
  "webDir": "build",
  "bundledWebRuntime": false
};

export default config;
