import React from "react";
import styled from "styled-components";

const MainFooter = () => {
  return <MainFooterStyle>MainFooter</MainFooterStyle>;
};

export default MainFooter;

const MainFooterStyle = styled.div`
  position: absolute;
  bottom: 0;
  left: 0;
  width: 100%;
  height: 60px;
`;
